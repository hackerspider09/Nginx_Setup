version: '3'
services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web

  # nginx_ssl:
  #   image: nginx:latest
  #   ports:
  #     - "443:443"
  #     - "80:80"
  #   volumes:
  #     - ./nginx/ssl.conf:/etc/nginx/conf.d/default.conf
  #     - ./nginx/certs:/etc/nginx/certs
  #   depends_on:
  #     - web
  #     - certbot

  web:
    build: ./web

  certbot:
    image: certbot/certbot
    volumes:
      - ./nginx/certs:/etc/nginx/certs
    entrypoint: "/scripts/create_ssl.sh"

